<?php

use App\Games\GuessNumberGameEntry;

require __DIR__.'/vendor/autoload.php';

$dotenv = Dotenv\Dotenv::createImmutable(__DIR__);
$dotenv->load();

$options = getopt("h::l:", ['help::', 'length:']);

$game = new GuessNumberGameEntry($options);
$game->init();

